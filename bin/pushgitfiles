#!/usr/bin/env bash

MESSAGE=$1

if [ -z "$MESSAGE" ];then
    MESSAGE="$HOSTNAME"
fi

echo "git add ."
git add .

echo "git commit -m $MESSAGE"
git commit -m "$MESSAGE"

git push -u origin master
exit 0
